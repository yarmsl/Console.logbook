interface Child  {
	children?: React.ReactNode;
}

interface MetaLT {
	title: string;
}

interface formLogin {
	email: string;
	password: string;
}

interface postProps {
	title: string;
	text: string;
}